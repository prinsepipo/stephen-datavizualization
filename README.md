# Setup

1. Install latest version of `Python3`. https://www.python.org/downloads/

2. Then run
    ```bash
    pip install -r requirements.txt
    ```

3. Run the program
    ```bash
    python main.py
    ```
